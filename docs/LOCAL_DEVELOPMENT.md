# Local Development Guide

> 🌐 **Язык:** [English](LOCAL_DEVELOPMENT_EN.md) | Русский

Полное руководство по развёртыванию Clipmaker на локальной машине (macOS/Linux).

---

## Содержание

1. [Предварительные требования](#1-предварительные-требования)
2. [Клонирование репозитория](#2-клонирование-репозитория)
3. [Установка FFmpeg](#3-установка-ffmpeg)
4. [Настройка Python и Whisper](#4-настройка-python-и-whisper)
5. [Настройка папки с видео](#5-настройка-папки-с-видео)
6. [Настройка Backend](#6-настройка-backend)
7. [Настройка Frontend](#7-настройка-frontend)
8. [Запуск приложения](#8-запуск-приложения)
9. [Проверка работоспособности](#9-проверка-работоспособности)
10. [Troubleshooting](#10-troubleshooting)

---

## 1. Предварительные требования

### Необходимое ПО

| Software | Версия | Проверка | Установка |
|----------|--------|----------|-----------|
| **Node.js** | 20+ | `node --version` | [nodejs.org](https://nodejs.org/) |
| **Python** | 3.11+ | `python3 --version` | [python.org](https://python.org/) |
| **FFmpeg** | 6+ | `ffmpeg -version` | См. раздел 3 |
| **Git** | любая | `git --version` | [git-scm.com](https://git-scm.com/) |

### API ключи

| Сервис | Назначение | Получить |
|--------|------------|----------|
| **OpenRouter** | AI анализ (Gemini) | [openrouter.ai/keys](https://openrouter.ai/keys) |

---

## 2. Клонирование репозитория

```bash
# Перейти в папку для проектов
cd ~/Projects  # или другая папка

# Клонировать репозиторий
git clone https://github.com/Serg1kk/clipmaker.git

# Перейти в папку проекта
cd clipmaker
```

### Структура проекта после клонирования

```
clipmaker/
├── backend/           # FastAPI сервер + Whisper
├── frontend/          # React приложение + nginx.conf
├── docker/            # Docker конфигурации (CLI whisper)
├── whisper/           # Whisper HTTP сервис (для production)
├── docs/              # Документация
├── videos/            # Папка для исходных видео
├── output/            # Результаты рендеринга
├── uploads/           # Временные загрузки
├── docker-compose.yml              # Development конфиг
├── docker-compose.production.yml   # Production конфиг (микросервисы)
├── README.md
└── .env.example
```

---

## 3. Установка FFmpeg

FFmpeg используется для:
- Извлечения аудио из видео (для Whisper)
- Рендеринга финальных клипов с субтитрами
- Конвертации форматов

### macOS (через Homebrew)

```bash
# Установить Homebrew если нет
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить FFmpeg
brew install ffmpeg

# Проверить установку
ffmpeg -version
# Должно показать: ffmpeg version 7.x.x ...
```

### Ubuntu/Debian Linux

```bash
sudo apt update
sudo apt install ffmpeg

# Проверить
ffmpeg -version
```

### Windows (WSL2)

```bash
# В WSL2 терминале
sudo apt update
sudo apt install ffmpeg
```

---

## 4. Настройка Python и Whisper

### Whisper - Модели транскрипции

Whisper - это нейросеть от OpenAI для распознавания речи. Работает **полностью локально**.

| Модель | Размер | RAM | Скорость* | Качество | Рекомендация |
|--------|--------|-----|-----------|----------|--------------|
| `tiny` | 75 MB | 4 GB | 32x | ⭐⭐ | Быстрые тесты |
| **`base`** | 142 MB | 4 GB | 16x | ⭐⭐⭐ | **Рекомендуется** |
| `small` | 466 MB | 6 GB | 6x | ⭐⭐⭐⭐ | Лучше качество |
| `medium` | 1.5 GB | 10 GB | 2x | ⭐⭐⭐⭐⭐ | Высокое качество |
| `large` | 2.9 GB | 16 GB | 1x | ⭐⭐⭐⭐⭐ | Максимальное качество |

*Скорость относительно реального времени на Apple M1

### Ускорение на разных платформах

| Платформа | GPU ускорение | Как работает |
|-----------|---------------|--------------|
| **Mac M1/M2/M3** | ✅ MPS (Metal) | Автоматически |
| **NVIDIA GPU** | ✅ CUDA | Автоматически |
| **CPU only** | ❌ | Медленнее в 3-5x |

### Создание виртуального окружения

> 📍 **Где вы сейчас:** Корень проекта `clipmaker/`

```bash
# Перейти в папку backend
cd backend

# Теперь вы в: clipmaker/backend/
```

> 📍 **Где вы сейчас:** `clipmaker/backend/`

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
# macOS/Linux:
source venv/bin/activate
# Windows:
# .\venv\Scripts\activate

# Проверить что активировано (в начале строки появится "(venv)")
which python
# Должно показать: .../clipmaker/backend/venv/bin/python
```

### Установка зависимостей

> 📍 **Где вы сейчас:** `clipmaker/backend/` с активным venv (в начале строки `(venv)`)

```bash
# Обновить pip
pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt
```

**Время установки:** ~5-10 минут (PyTorch большой)

### Первый запуск Whisper (скачивание модели)

При первом использовании Whisper автоматически скачает модель:

> 📍 **Где вы сейчас:** `clipmaker/backend/` с активным venv

```bash
# Тестовый запуск для скачивания модели
python -c "import whisper; whisper.load_model('base')"
```

Модель сохраняется в `~/.cache/whisper/` (~150MB для base)

---

## 5. Настройка папки с видео

> ⚠️ **Важно:** Папка `videos/` уже существует в репозитории после клонирования! Создавать её не нужно.

> 📍 **Где вы сейчас:** `clipmaker/backend/` — нужно вернуться в корень проекта

```bash
# Вернуться в корень проекта
cd ..

# Теперь вы в: clipmaker/
```

### Проверка папки videos

> 📍 **Где вы сейчас:** Корень проекта `clipmaker/`

```bash
# Папка videos уже существует, просто проверьте
ls -la videos/

# Если папка пуста - добавьте тестовое видео
cp ~/Downloads/test-video.mp4 videos/
```

### Альтернативные варианты (опционально)

Если хотите использовать другую папку для видео:

#### Вариант B: Системная папка Videos

```bash
# Создать папку если нет
mkdir -p ~/Videos/clipmaker-source

# В .env указать путь (см. раздел 6)
VIDEO_SOURCE_PATH=~/Videos/clipmaker-source
```

#### Вариант C: Симлинк на существующую папку

```bash
# Удалить пустую папку videos и создать ссылку на вашу папку
rm -rf videos
ln -s /путь/к/вашим/видео ./videos
```

### Проверка доступа

```bash
# Проверить что папка содержит видео
ls -la videos/

# Должно показать ваши видео файлы
# -rw-r--r--  1 user  staff  50000000 Dec 27 10:00 video.mp4
```

---

## 6. Настройка Backend

### Создание конфигурационного файла

> 📍 **Где вы сейчас:** Корень проекта `clipmaker/` (после раздела 5)

```bash
# Создать .env файл в корне проекта
cat > .env << 'EOF'
# ============================================
# API Keys
# ============================================
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь

# ============================================
# Video Source
# ============================================
# Вариант A: папка в проекте
VIDEO_SOURCE_PATH=./videos

# Вариант B: системная папка
# VIDEO_SOURCE_PATH=~/Videos/ai-clips-source

# ============================================
# Whisper Configuration
# ============================================
# Модели: tiny, base, small, medium, large
WHISPER_MODEL=base

# ============================================
# Processing Settings
# ============================================
MAX_CONCURRENT_JOBS=2
CLIP_MIN_DURATION=13
CLIP_MAX_DURATION=60

# ============================================
# FFmpeg Settings
# ============================================
# Presets: ultrafast, superfast, fast, medium, slow
FFMPEG_PRESET=medium
# CRF: 18 (лучшее качество) - 28 (меньше размер)
FFMPEG_CRF=23
EOF
```

### Получение OpenRouter API ключа

1. Зайти на [openrouter.ai](https://openrouter.ai/)
2. Зарегистрироваться/войти
3. Перейти в [API Keys](https://openrouter.ai/keys)
4. Нажать "Create Key"
5. Скопировать ключ (начинается с `sk-or-v1-`)
6. Вставить в `.env` файл

---

## 7. Настройка Frontend

> 📍 **Где вы сейчас:** Корень проекта `clipmaker/`

```bash
# Перейти в папку frontend
cd frontend

# Теперь вы в: clipmaker/frontend/

# Установить зависимости
npm install
```

**Время установки:** ~2-3 минуты

> 📍 **После установки:** Оставайтесь в `clipmaker/frontend/` — скоро запустим frontend отсюда

---

## 8. Запуск приложения

> 🖥️ **Вам понадобятся 2 терминала** — один для backend, другой для frontend. Они работают параллельно.

---

### 🖥️ ТЕРМИНАЛ 1: Запуск Backend

> Откройте **первый терминал** (или используйте текущий)

```bash
# 1. Перейти в корень проекта (замените путь на свой)
cd ~/Projects/clipmaker

# 2. Активировать виртуальное окружение Python
source backend/venv/bin/activate
# В начале строки появится (venv)

# 3. Перейти в папку backend
cd backend

# 4. Запустить сервер
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

> 📍 **Итог:** Терминал 1 находится в `clipmaker/backend/` и запущен uvicorn

Вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [12345]
INFO:     Started server process [12346]
INFO:     Application startup complete.
```

> ⚠️ **Не закрывайте этот терминал!** Сервер должен работать постоянно.

---

### 🖥️ ТЕРМИНАЛ 2: Запуск Frontend

> Откройте **второй терминал** (Cmd+T в macOS или новая вкладка)

```bash
# 1. Перейти сразу в папку frontend (замените путь на свой)
cd ~/Projects/clipmaker/frontend

# 2. Запустить dev сервер
npm run dev
```

> 📍 **Итог:** Терминал 2 находится в `clipmaker/frontend/` и запущен Vite

Вы увидите:
```
  VITE v5.x.x  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
```

> ⚠️ **Не закрывайте этот терминал!** Frontend сервер должен работать постоянно.

---

### Схема работы

```
┌─────────────────────────────────────────────────────────────┐
│                        ВАШ КОМПЬЮТЕР                         │
├─────────────────────────────┬───────────────────────────────┤
│      🖥️ ТЕРМИНАЛ 1          │       🖥️ ТЕРМИНАЛ 2           │
│                             │                               │
│  clipmaker/backend/         │  clipmaker/frontend/          │
│  (venv) активен             │                               │
│                             │                               │
│  uvicorn main:app --reload  │  npm run dev                  │
│  ↓                          │  ↓                            │
│  Backend API :8000          │  Frontend :5173               │
└─────────────────────────────┴───────────────────────────────┘
                              │
                              ▼
                    🌐 Браузер: localhost:5173
```

---

## 9. Проверка работоспособности

### Проверить Backend API

```bash
# Health check
curl http://localhost:8000/health

# Ожидаемый ответ:
# {"status":"healthy","timestamp":"...","version":"1.0.0"}

# Проверить список видео
curl http://localhost:8000/files

# API документация
open http://localhost:8000/docs
```

### Проверить Frontend

1. Открыть в браузере: http://localhost:5173/
2. Должна загрузиться главная страница
3. Проверить что видео из папки отображаются

### Тест транскрипции

1. Выбрать видео в интерфейсе
2. Нажать "Transcribe"
3. Дождаться завершения (прогресс в реальном времени)
4. Проверить результат с таймстампами

---

## 10. Troubleshooting

### Whisper не находит GPU (Mac)

```bash
# Проверить доступность MPS
python -c "import torch; print(f'MPS available: {torch.backends.mps.is_available()}')"

# Если False - обновить PyTorch
pip install --upgrade torch torchvision torchaudio
```

### FFmpeg не найден

```bash
# Проверить установку
which ffmpeg
# Должен показать путь, например /opt/homebrew/bin/ffmpeg

# Если пусто - переустановить
brew reinstall ffmpeg
```

### Ошибка "Module not found"

```bash
# Убедиться что виртуальное окружение активно
which python
# Должно показать путь в venv, НЕ /usr/bin/python

# Если нет - активировать
source backend/venv/bin/activate
```

### Порт 8000 занят

```bash
# Найти процесс
lsof -i :8000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
uvicorn main:app --reload --port 8001
```

### Видео не отображаются

```bash
# Проверить папку videos
ls -la videos/

# Проверить права доступа
chmod 755 videos/
chmod 644 videos/*.mp4

# Проверить что формат поддерживается
# Поддерживаются: .mp4, .mov, .avi, .mkv, .webm, .m4v
```

### Ошибка OpenRouter API

```bash
# Проверить что ключ установлен
echo $OPENROUTER_API_KEY
# или
grep OPENROUTER .env

# Проверить баланс на openrouter.ai/credits
```

---

## Быстрый старт (TL;DR)

```bash
# 1. Клонировать
git clone https://github.com/Serg1kk/clipmaker.git && cd clipmaker

# 2. Установить FFmpeg (Mac)
brew install ffmpeg

# 3. Настроить backend (создать venv, установить зависимости)
cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# 4. Создать .env в корне проекта
cd .. && cp .env.example .env
# Отредактировать .env - добавить OPENROUTER_API_KEY

# 5. Добавить тестовое видео (папка videos/ уже существует)
cp ~/Downloads/test.mp4 videos/

# 6. Установить frontend зависимости
cd frontend && npm install && cd ..
```

### Запуск (нужны 2 терминала!)

```bash
# 🖥️ ТЕРМИНАЛ 1 - Backend
cd ~/путь/к/clipmaker
source backend/venv/bin/activate
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 🖥️ ТЕРМИНАЛ 2 - Frontend (новый терминал!)
cd ~/путь/к/clipmaker/frontend
npm run dev

# 🌐 Открыть в браузере: http://localhost:5173/
```

---

## Полезные команды

```bash
# Перезапустить backend
# Ctrl+C в терминале backend, затем:
uvicorn main:app --reload

# Очистить кэш Whisper моделей
rm -rf ~/.cache/whisper/

# Посмотреть логи в реальном времени
tail -f backend/logs/app.log  # если настроено логирование

# Проверить использование памяти
top -pid $(pgrep -f uvicorn)
```
